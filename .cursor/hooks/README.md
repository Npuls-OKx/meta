# Cursor hooks

Scripts die rond agent-acties draaien om beleid af te dwingen, bijvoorbeeld het **blokkeren van pakket-installaties buiten de dev container**. De koppeling van events aan scripts staat in [`.cursor/hooks.json`](../hooks.json).
